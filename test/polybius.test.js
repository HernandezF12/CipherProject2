// Write your tests here!
const polybius = require("../src/polybius");

describe("polybuis()", () => {
    it("when encoding output should be a string.", () => {
        //testing code goes here
    })
    it("both letters share a space, be converted to 42 and decoded to their own letters properly.", () => {
        // testing code goes here 
    })
    it("when encoding output should still be a string.", () => {
        //testing code goes here 
    })
    it("when decoding the number of characters in the string excluding spaces should be even, otherwise return false.", () => {
        //testing code goes here
    })
    it("checks if letters I and J share the same number when encoded.", () => {
        //testing code goes here 
    })
}) 
